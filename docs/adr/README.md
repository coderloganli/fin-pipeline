# Architecture decision records

One file per decision, recording the context, the choice, and what was rejected.

Design-time decisions already made and worth recording here: PySpark rather than Scala, dbt and Spark holding separate responsibilities, Postgres for serving with Parquet for detail, batch rather than streaming, and synthetic data rather than a public dataset. Each will be written up as the corresponding module lands.
